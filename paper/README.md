# Manuscript Draft

`Prefix_Rigidity_Collatz_Circuits.pdf` is a manuscript draft.

Important repository-status note: the PDF describes an intended complete
supplementary release with raw logs, immutable manifests, `verify_all.py`,
adversarial tests, and additional certificate files. Those release artifacts are
not all present in this repository snapshot. The root `README.md` is the source
of truth for the current executable status.
